name: responsive_spacing_example
publish_to: 'none'
description: Example app demonstrating the responsive_spacing package
version: 1.0.0+1

environment:
  sdk: ^3.9.0
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  responsive_spacing_plus:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
